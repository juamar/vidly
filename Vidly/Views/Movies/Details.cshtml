@model Vidly.Models.Movies

@{
    ViewBag.Title = "Movies";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>@Model.name</h2>
<pre>
    Id = @Model.id
    Genre = @Model.Genre.Name
    Release Date = ((Date)@Model.ReleaseDate).ToString("MM/dd/yyyy")
    Date Added = ((Date)@Model.DateAdded).ToString("MM/dd/yyyy")
    In Stock = @Model.NumberInStock
</pre>